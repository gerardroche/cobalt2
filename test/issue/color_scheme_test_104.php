<?php // COLOR TEST "Packages/Theme - Cobalt2/cobalt2.tmTheme" "PHP"

if ( false === $i ) {
    if ( ! $gmt )
        $i = current_time( 'timestamp' );
//              ^ fg=#ffee80
    else
        $i = time();
}
